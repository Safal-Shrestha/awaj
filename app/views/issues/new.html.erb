<% content_for :title, "New issue" %>

<div class="max-w-xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold text-brand mb-6">New Issue</h1>

  <%= render "form", issue: @issue %>

  <div class="mt-6">
    <%= link_to "← Back to issues", issues_path, class: "text-sm text-zinc-500 hover:text-brand" %>
  </div>
</div>