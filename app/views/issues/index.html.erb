<h1>Issues</h1>

<% if policy(Issue).create? %>
  <%= link_to "Report an Issue", new_issue_path, class: "btn-primary" %>
<% end %>

<ul>
  <% @issues.each do |issue| %>
    <li>
      <%= link_to issue.title, issue %>
      — <%= issue.aasm_state.humanize %>
    </li>
  <% end %>
</ul>